CREATE DATABASE IF NOT EXISTS diccionario;
USE diccionario
CREATE TABLE IF NOT EXISTS palabras1(
   palabra varchar(15),
   significado varchar(255)
);

CREATE TABLE IF NOT EXISTS palabras2(
   palabra varchar(15),
   significado varchar(255)
);

